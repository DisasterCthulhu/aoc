inherit "/w/elronuan/aoc/2021/helper";

int part1(mixed x) {
  return 0;
}

void configure() {
  ::configure();
  set_day(x);
  int array samples = to_ints(to_lines(input("sample")));
  //int array values =
  dbg(part1(samples));
  //dbg(part1(values));
  //dbg(part2(samples));
  //dbg(part2(values));
}
